import { View, Text } from "react-native";

const Authentification = ({navigation}) => {
    return (
        <View>
            <Text>Bite</Text>
        </View>
    );
}
export default Authentification;