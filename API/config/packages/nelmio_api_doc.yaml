nelmio_api_doc:
    documentation:
        info:
            title: My App
            description: This is an awesome app!
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api/user # Accepts routes under /api except /api/doc
            - ^/api/collocation # Accepts routes under /api except /api/doc
            - ^/api/invitation # Accepts routes under /api except /api/doc
